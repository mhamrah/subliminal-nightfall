name: Publish Cursor Theme (VS Code + Open VSX)

on:
  push:
    branches:
      - main
    paths:
      - 'cursor/**'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  publish:
    name: Publish to both registries
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install vsce and ovsx
        run: npm install -g @vscode/vsce ovsx

      - name: Auto-bump patch version
        working-directory: cursor
        run: |
          node -e "const fs=require('fs'); const p=JSON.parse(fs.readFileSync('package.json','utf8')); const v=p.version.split('.').map(Number); if(v.length!==3||v.some(n=>Number.isNaN(n))) throw new Error('Invalid semver in package.json'); v[2]++; p.version=v.join('.'); fs.writeFileSync('package.json', JSON.stringify(p,null,2)+'\n'); console.log('New version:', p.version);"

      - name: Publish to VS Code Marketplace
        working-directory: cursor
        env:
          VSCE_PAT: ${{ secrets.VSCE_TOKEN }}
        run: vsce publish --pat "$VSCE_PAT" --baseContentUrl https://github.com/mhamrah/subliminal-nightfall/raw/main/cursor --baseImagesUrl https://github.com/mhamrah/subliminal-nightfall/raw/main/cursor

      - name: Publish to Open VSX Registry
        working-directory: cursor
        env:
          OVSX_PAT: ${{ secrets.OPEN_VSX_TOKEN }}
        run: ovsx publish --pat "$OVSX_PAT" --baseContentUrl https://github.com/mhamrah/subliminal-nightfall/raw/main/cursor --baseImagesUrl https://github.com/mhamrah/subliminal-nightfall/raw/main/cursor

      - name: Commit and push version bump
        working-directory: cursor
        run: |
          set -euo pipefail
          NEW_VERSION=$(node -p "require('./package.json').version")
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add package.json
          git commit -m "chore(cursor): bump version to ${NEW_VERSION} [skip ci]"
          git push
